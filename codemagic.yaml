workflows:
  android-release:
    name: Android Release Build
    environment:
      flutter: stable
    scripts:
      - cd organic-bloom
      - flutter pub get
      - flutter build apk --release
    artifacts:
      - build/app/outputs/flutter-apk/*
